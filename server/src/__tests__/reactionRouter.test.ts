describe("reactionRouter tests", () => {
  it("should add a like reaction to a post", async () => {});
  it("should add a dislike reaction to a post", async () => {});
  it("should add a like reaction to a comment", async () => {});
  it("should add a dislike reaction to a comment", async () => {});
  it("should remove a like reaction from a post", async () => {});
  it("should remove a dislike reaction from a post", async () => {});
  it("should remove a like reaction from a comment", async () => {});
  it("should remove a dislike reaction from a comment", async () => {});
  it("should return 404 if the post does not exist", async () => {});
  it("should return 404 if the comment does not exist", async () => {});
  it("should return 401 if the user is a guest", async () => {});
});
